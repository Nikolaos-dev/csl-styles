<?xml version="1.0" encoding="utf-8"?>
<style xmlns="http://purl.org/net/xbiblio/csl" version="1.0">
  <info>
    <title>Techniques de l'ingénieur — Numérique FR</title>
    <id>http://www.zotero.org/styles/techniques-ingenieur</id>
    <link href="http://www.zotero.org/styles/techniques-ingenieur" rel="self"/>
    <author>
      <name>Generated by ChatGPT</name>
    </author>
    <category citation-format="numeric"/>
    <category field="engineering"/>
    <summary>Style numérique pour références Techniques de l'ingénieur, noms en majuscules avec initiales entre parenthèses, pas de résumé.</summary>
  </info>

  <!-- Macro pour les auteurs -->
  <macro name="authors">
    <names variable="author" delimiter=", " et-al-min="0" et-al-use-first="1">
      <name form="long" initialize-with="." sort-separator=", ">
        <name-part name="family" text-case="uppercase"/>
        <name-part name="given" form="short" prefix="(" suffix=")"/>
      </name>
      <label form="short" prefix=" et "/>
    </names>
  </macro>

  <!-- Macro pour titre -->
  <macro name="title">
    <text variable="title" text-case="capitalize-first"/>
  </macro>

  <!-- Macro pour revue / livre -->
  <macro name="container">
    <choose>
      <if type="article-journal">
        <text variable="container-title" prefix="– "/>
      </if>
      <else-if type="book">
        <text variable="publisher" prefix="– "/>
        <text variable="publisher-place" prefix=", "/>
      </else-if>
    </choose>
  </macro>

  <!-- Macro pour volume, pages et année -->
  <macro name="volume-pages-year">
    <choose>
      <if type="article-journal">
        <text variable="volume" prefix=", "/>
        <text variable="page" prefix=", p. "/>
        <date variable="issued" form="text" prefix=" (" suffix=")"/>
      </if>
      <else-if type="book">
        <date variable="issued" form="text" prefix=" (" suffix=")"/>
      </else-if>
    </choose>
  </macro>

  <!-- Citation numérique [1], [2], … -->
  <citation>
    <layout prefix="[" suffix="]" delimiter=", ">
      <text variable="citation-number"/>
    </layout>
  </citation>

  <!-- Bibliographie -->
  <bibliography>
    <layout suffix=".">
      <text macro="authors"/>
      <text macro="title" prefix=" "/>
      <text macro="container"/>
      <text macro="volume-pages-year"/>
    </layout>
  </bibliography>
</style>
